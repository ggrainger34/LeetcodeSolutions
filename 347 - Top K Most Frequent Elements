"""
Given an integer array nums and an integer k, return the k most frequent elements. You may return the answer in any order.

Example 1:

Input: nums = [1,1,1,2,2,3], k = 2

Output: [1,2]

Example 2:

Input: nums = [1], k = 1

Output: [1]

Example 3:

Input: nums = [1,2,1,2,1,2,3,1,3,2], k = 2

Output: [1,2]
"""

class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        # cnt is a dictionary of how many times each element appears
        cnt = Counter(nums)
        # Number of appearances of kth most element
        cntValues = sorted(list(cnt.values()), reverse=True)[0:k]
        keys = []
        for key, val in cnt.items():
            if val in cntValues:
                keys.append(key)
        return keys